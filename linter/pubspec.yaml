name: io_mindtechapps_hw_custom_lint
version: 0.0.1

publish_to: "none" # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  analyzer: 7.3.0
  custom_lint_builder: ^0.7.5
  custom_lint: ^0.7.5
